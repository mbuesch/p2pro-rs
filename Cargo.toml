[package]
name = "p2pro-rs"
version = "1.0.0"
edition = "2024"
rust-version = "1.91"
license = "MIT"
authors = [ "Michael Büsch <m@bues.ch>" ]
homepage = "https://bues.ch/"
repository = "https://github.com/mbuesch/p2pro-rs"
readme = "README.md"
categories = [ "graphics", "hardware-support", "science" ]
keywords = [ "infiray", "p2pro", "thermal", "camera" ]

[dependencies]
anyhow = "1"
base64 = "0.23"
chrono = "0.4"
clap = { version = "4", features = [ "derive" ] }
dioxus = { version = "0.7", features = [ "desktop", "mobile" ] }
image = { version = "0.25", default-features = false, features = [ "png" ] }
movavg = "2"
tokio = { version = "1", default-features = false, features = [ "sync", "fs", "rt-multi-thread" ] }

[target.'cfg(target_os = "linux")'.dependencies]
rfd = { version = "0.17", default-features = false, features = [ "xdg-portal" ] }
v4l = "0.14"

[target.'cfg(target_os = "android")'.dependencies]
android_logger = "0.15"
jni = "0.22"
libc = "0.2"
log = "0.4"
rusb = { version = "0.9", features = [ "vendored" ] }

[profile.dev]
debug = "limited"
panic = "abort"

[profile.release]
lto = "thin"
panic = "abort"
strip = "symbols"
